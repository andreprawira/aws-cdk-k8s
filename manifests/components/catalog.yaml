apiVersion: v1
kind: Pod
metadata:
  name: ui
  namespace: ui
spec:
  containers:
  - name: ui
    image: public.ecr.aws/aws-containers/retail-store-sample-catalog:0.8.3
    ports:
    - containerPort: 80
